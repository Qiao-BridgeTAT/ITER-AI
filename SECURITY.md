# 安全问题反馈

当前为源码候选版，不提供稳定生产支持、安全认证或固定响应时限的承诺。

如发现越权访问、敏感信息泄露、认证绕过或其他疑似漏洞，请私下联系：

**[Li.qiao02@outlook.com](mailto:Li.qiao02@outlook.com)**

建议说明受影响的公开提交、最小复现步骤、预期行为与实际影响。示例必须脱敏；不要发送真实用户数据库、完整对话、手机号、Cookie、验证码、密钥或未脱敏的 Provider 响应。必要的更多材料请先沟通安全传递方式。

不要在公开 Issue 或 PR 中直接披露可利用的漏洞细节。如果凭据已经泄露，请先在对应服务商处撤销或轮换，删除公开文字并不能让旧凭据失效。

自部署者负责 HTTPS、最小权限、限流、备份、依赖更新、日志保留和用户数据保护。不要将开发服务、数据库、Redis 或 `.local/` 目录直接暴露到公网；不要把服务端秘密配置放入 `VITE_*`。
